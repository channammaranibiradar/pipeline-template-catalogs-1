type: pipeline-template
version: 1

name: Java CI Pipeline 
displayName: Java CI – Build, Scan & Docker Image

description: >
  Build Java apps, run SonarQube, Trivy scans,
  build Docker images using Kaniko, and generate SBOM.

parameters:
  - name: repoUrl
    type: string
    displayName: Git Repository URL
    required: true

  - name: branchName
    type: string
    displayName: Git Branch
    defaultValue: main

  - name: gitCredentials
    type: credentials
    displayName: Git Credentials
    required: true

  - name: imageName
    type: string
    displayName: Docker Image Name
    defaultValue: gkamalakar1006/boardgames

  - name: sonarProjectKey
    type: string
    displayName: SonarQube Project Key
    defaultValue: board_game
